{
  "name": "nightwatch",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:debug": "mkdirp report && cucumber-js --require cucumber.conf.js  --tags @debug --require src/step_definitions src/features --tags @debug --format json:report/run.json",
    "test:chromeDaily": "mkdirp report && cucumber-js --require cucumber.conf.js --tags @SmokeTests --require src/step_definitions src/features --format json:report/run.json",
    "test:chromeWeekly": "mkdirp report && cucumber-js --require cucumber.conf.js --require src/step_definitions src/features --format json:report/run.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "cucumber-html-reporter": "^7.2.0",
    "mkdirp": "^1.0.4",
    "nightwatch": "^3.12.0"
  }
}
